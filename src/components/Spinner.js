
import Loader from "./Rainbow.gif";
const Spinner = () => {
  return (
    <div className="text-center load">
      <img className="load" src={Loader} alt="loader" />
    </div>
  );
};

export default Spinner;
